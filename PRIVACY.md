# 隐私政策 — Audiobookshelf

> 适用审核项：C3（隐私政策有无）、C4（完整性）、C5（可访问）、C6（数据收集一致性）、C7（用户数据权利）、C8（第三方 API 说明）

**生效日期**：2026-09-18　**最近更新**：2026-09-21
**适用版本**：1.0.006
**应用形态**：TOS 7 Docker 应用（Compose）

## 一、数据收集与处理

Audiobookshelf 是**自托管**的有声书与播客服务器，本应用不向开发者收集任何数据。

- 媒体文件、封面、元数据、用户账号与收听进度，全部保存在**本机挂载目录**：
  - `/Volume*/DockerAppData/shh5-audiobookshelf/data` → 容器内 `/config`（数据库、账号、配置）
  - `/Volume*/DockerAppData/shh5-audiobookshelf/metadata` → 容器内 `/metadata`（封面、缓存、转码产物）
- 启用**元数据刮削**时会向所选元数据服务查询书名 / 作者 / ISBN 等信息
- 启用**播客订阅**时会定期访问所订阅的播客 RSS 源
- **本封装不含任何统计、埋点、遥测或回传代码**，开发者不持有、不接触任何用户数据
- 卸载时若勾选「同时删除数据」，上述目录将被清除

## 二、数据保存期限

- 媒体库、账号、收听进度保存在本机挂载目录中，**不设自动过期**，保留时长由用户自行管理
- 用户在 WebUI 中删除媒体项 / 用户后即被清除
- 卸载应用并选择「同时删除数据」后即被清除
- 开发者侧**不存在**任何副本，故无需另行约定开发者侧的保存期限

## 三、第三方共享与数据流向（C8）

**开发者不与任何第三方共享数据**（因为开发者根本不接收数据）。

仅在管理员启用相应功能时，才会向以下**由管理员选择**的第三方服务发起请求：

| 功能 | 典型第三方服务 | 发送的数据 | 数据去向 / 地域 |
|------|--------------|-----------|----------------|
| 元数据刮削 | Audnexus、Apple Books(iTunes)、Audible、Google Books、Open Library、FantLab、Big Book API 等 | 书名 / 作者 / ISBN / ASIN 等**检索词** | 对应服务商的服务器所在地（多为美国 / 欧盟） |
| 播客订阅 | 管理员添加的播客 RSS 源 | 常规 HTTP 请求（含客户端 IP） | 该 RSS 源托管方所在地 |
| 推送通知（如启用） | 管理员配置的 Apprise / 移动端推送服务 | 通知标题与正文 | 对应服务商的服务器所在地 |

- **传输地域**：取决于管理员所选服务商所在地，开发者不参与、不中转、不可见
- **不刮削、不订阅播客时**，应用仅在局域网内提供 WebUI 与媒体流，不产生对外数据发送
- 本应用不含广告、分析或用户画像类第三方 SDK

## 四、数据安全措施（本应用的实际机制）

本应用以 **Docker 容器**方式运行，安全机制来自 `docker-compose.yml`，**不涉及 systemd**：

- **非 root 运行**：容器以 `user: "1000:1000"` 运行，不以 root 身份执行
- **禁止提权**：`security_opt: no-new-privileges:true`
- **不使用特权模式**：无 `privileged: true`
- **不使用 host 网络**：走 bridge 网络 + 端口映射，仅对外暴露 `18805`
- **资源上限**：`cpus: "1.0"`、`memory: 1G`
- **镜像锁定**：`advplyr/audiobookshelf:2.36.1`（具体版本，不使用 `:latest`）
- **健康检查**：容器内置 healthcheck，异常时由 Docker 自动重启
- 账号密码以哈希形式存储于本机 SQLite 数据库，不经由开发者服务器中转

## 五、用户数据权利（C7）

用户可随时查阅、更正、删除自己的数据：

1. **查阅 / 更正**：通过应用自身的 WebUI 操作（媒体库、用户、收听进度均可编辑）
2. **删除**：删除目录 `/Volume*/DockerAppData/shh5-audiobookshelf/`，或在 TOS 应用中心卸载时勾选「同时删除数据」
3. **完全移除**：卸载应用并确认删除上述数据目录，本机即不再保留任何本应用数据

## 六、联系方式

如对隐私政策有疑问，请通过上游项目仓库提交 issue：
<https://github.com/advplyr/audiobookshelf/issues>

本封装仓库（TOS 平台集成部分）的问题请提交到：
<https://github.com/RyanYang163/shh5-audiobookshelf/issues>
